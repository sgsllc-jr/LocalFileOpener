# LocalFileOpener
Opens local files on a Windows PC. Useful if you want to enable links in your browser that open local files.

## Installation
1. Install this executable on your Windows machine
2. Add a Registry key to set the executable as the default handler for your custom protocol
Example:

